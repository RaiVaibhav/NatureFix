// Single source of truth for the deployed origin. Swap this when a custom
// domain replaces the Netlify subdomain — every SEO file below reads from here.
export const SITE_URL = 'https://naturefix.in'
